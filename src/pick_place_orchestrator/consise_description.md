# Pick Place Orchestrator

This package acts as the Master Orchestrator, executing a 12-step autonomous pick and place loop. It uses `behaviortree_cpp` to coordinate navigation, base placement, and arm manipulation tasks.
